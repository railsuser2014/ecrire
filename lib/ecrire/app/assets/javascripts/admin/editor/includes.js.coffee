# Since the ordering is important in the editor and JS is too stupid to have
# any mechanics that would make this trivial, I need to create a file that
# will require JS in the specific order
#
#= require admin/editor/content
#= require_tree ./parsers/
